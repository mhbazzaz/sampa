You are an expert NestJS backend engineer. Implement a robust, database-backed system for tracking changes across multiple API calls before they are permanently logged. This system must ensure zero data loss, even during application restarts or crashes.

Context
We have an existing action logging system that currently uses an in-memory buffer. We need to replace it with a database-backed solution using a PendingChange entity. The system tracks changes to assessment requests, layers, testcases, specs, and remediations, then flushes them to an ActionLog when a state transition occurs.

Existing Code Structure
Entity: ActionLog (already exists)
Service: ActionLogBufferService (needs modification)
Service: GenericChangelogService (already exists - enriches changes)
Service: ChangelogConfigFactory (already exists - provides configs)
Types: ChangeLog, ChangelogConfig, ActionEnum, ActionLogStatusEnum
Implementation Requirements
1. Create PendingChange Entity
File: src/action-log/entities/pending-change.entity.ts

Create a new TypeORM entity with these specifications:

Fields:

id: UUID primary key, auto-generated
assessmentRequestId: UUID, required, indexed
assessmentLayerId: UUID, nullable, indexed
entityType: varchar, required (values: ‘request’ | ‘layer’ | ‘testcase’ | ‘spec’ | ‘remediate’)
beforeEntity: jsonb, required (stores the entity state before changes)
updateDto: jsonb, required (stores the update DTO)
userId: varchar/uuid, required (user who made the change)
ipAddress: varchar, nullable
createdAt: timestamp, auto-generated
isFlushed: boolean, default false, indexed
Indexes:

assessmentRequestId
assessmentLayerId
isFlushed
createdAt
Table name: pending_changes

2. Create Database Migration
File: src/migrations/[timestamp]-create-pending-changes-table.ts

Generate a TypeORM migration that:

Creates the pending_changes table with all columns
Adds all required indexes
Includes proper up() and down() methods
Uses uuid_generate_v4() for UUID generation (ensure uuid extension is enabled)
Important:

Column names should use camelCase in entity but snake_case in database
Use proper TypeORM column types: uuid, varchar, jsonb, timestamp, boolean
Add indexes for query performance
3. Update ActionLogBufferService
File: src/action-log/services/action-log-buffer.service.ts

Current Implementation Issues:

Uses in-memory Map for storing pending changes
Has timer-based auto-flush logic
Data is lost on application restart
Required Changes:

A. Remove In-Memory Storage
Remove the Map that stores pending changes
Remove all timer-related logic (setTimeout, clearTimeout)
Remove OnModuleDestroy lifecycle hook if only used for timers
B. Inject PendingChange Repository
typescript
constructor(
  @InjectRepository(ActionLog)
  private actionLogRepo: Repository<ActionLog>,
  @InjectRepository(PendingChange)
  private pendingChangeRepo: Repository<PendingChange>,
  private genericChangelogService: GenericChangelogService,
  private changelogConfigFactory: ChangelogConfigFactory,
) {}
C. Update addChange Method
Current signature (approximate):

typescript
addChange(key: BufferKey, entity: string, beforeEntity: any, updateDto: any): void
New signature:

typescript
async addChange(
  key: {
    assessmentRequestId: string;
    assessmentLayerId?: string;
  },
  details: {
    entityType: 'request' | 'layer' | 'testcase' | 'spec' | 'remediate';
    beforeEntity: any;
    updateDto: any;
    userId: string;
    ipAddress?: string;
  },
): Promise<void>
Implementation:

Save the change to pending_changes table using pendingChangeRepo.save()
Set isFlushed: false
Handle assessmentLayerId as null if not provided
Add error handling with logging
Add debug log: “Pending change added for Request: {id}, Layer: {id}”
D. Create getPendingChanges Private Method
typescript
private async getPendingChanges(key: {
  assessmentRequestId: string;
  assessmentLayerId?: string;
}): Promise<PendingChange[]>
Implementation:

Build where clause:
assessmentRequestId = key.assessmentRequestId
isFlushed = false
If key.assessmentLayerId is provided: assessmentLayerId = key.assessmentLayerId
If key.assessmentLayerId is NOT provided: assessmentLayerId IS NULL (use TypeORM’s IsNull())
Order by createdAt ASC (process changes in chronological order)
Add error handling with logging
Add debug log: “Found {count} pending changes for Request: {id}, Layer: {id}”
E. Update flushToActionLog Method
Current signature (approximate):

typescript
async flushToActionLog(key: BufferKey, logData: {...}): Promise<ActionLog>
Keep similar signature but ensure logData includes:

typescript
logData: {
  userId: string;
  ipAddress?: string;
  roleIds: string[];
  action: ActionEnum;
  assessmentRequestCurrentStateId?: string | null;
  assessmentRequestNextStateId?: string | null;
  assessmentLayerCurrentStateId?: string | null;
  assessmentLayerNextStateId?: string | null;
  status: ActionLogStatusEnum;
}
Implementation Steps:

Fetch pending changes:
typescript
   const pendingChanges = await this.getPendingChanges(key);
Handle empty case:
If pendingChanges.length === 0:
Log: “No pending changes to flush for Request: {id}, Layer: {id}. Creating ActionLog for state transition only.”
Create and save ActionLog with changes: undefined or changes: null
Return the created ActionLog
Process each pending change:
Initialize empty array: const allChanges: ChangeLog[] = [];
Loop through each pendingChange:
Get config: const config = this.getConfigForEntity(pendingChange.entityType);
Enrich changes:
typescript
       const enrichedChanges = await this.genericChangelogService.buildAndEnrichChangeLog(
         pendingChange.beforeEntity,
         pendingChange.updateDto,
         config,
       );
Add to array: allChanges.push(...enrichedChanges);
Create ActionLog:
typescript
   const actionLog = await this.actionLogRepo.save({
     ...logData,
     assessmentRequestId: key.assessmentRequestId,
     assessmentLayerId: key.assessmentLayerId || null,
     changes: allChanges.length > 0 ? allChanges : undefined,
   });
Mark pending changes as flushed:
typescript
   await this.pendingChangeRepo.update(
     { id: In(pendingChanges.map(pc => pc.id)) },
     { isFlushed: true }
   );
Import In from TypeORM
Add logging:

Success log: “Flushed {count} pending changes to ActionLog {actionLogId}”
Error handling:

Wrap in try-catch
Log errors with stack trace
Consider transaction if needed (optional enhancement)
Return the created ActionLog

F. Create getConfigForEntity Private Method
typescript
private getConfigForEntity(
  entityType: 'request' | 'layer' | 'testcase' | 'spec' | 'remediate',
): ChangelogConfig

**Implementation**:
- Use switch statement on `entityType`
- Return appropriate config from `changelogConfigFactory`:
  - `'request'` → `getAssessmentRequestConfig()`
  - `'layer'` → `getAssessmentLayerConfig()`
  - `'testcase'` → `getTestcaseItemConfig()`
  - `'spec'` → `getRequestSpecItemConfig()`
  - `'remediate'` → `getTestcaseRemediateConfig()`

#### G. Create `cleanupFlushedChanges` Method

typescript
async cleanupFlushedChanges(olderThanDays: number = 7): Promise<number>

**Implementation**:
- Calculate cutoff date: `new Date()` minus `olderThanDays`
- Delete records where:
  - `isFlushed = true`
  - `createdAt < cutoffDate`
- Use TypeORM's `LessThan()` operator
- Return count of deleted records
- Add logging: "Cleaned up {count} flushed pending changes older than {days} days"

**Usage**: This should be called by a scheduled job (cron) to prevent table bloat.

### 4. Update ActionLogModule

**File**: `src/action-log/action-log.module.ts`

**Required Changes**:
- Add `PendingChange` to `TypeOrmModule.forFeature([...])`
- Ensure `ActionLogBufferService` is in `providers` array
- Ensure `ActionLogBufferService` is in `exports` array if used by other modules

**Example**:
typescript
@Module({
  imports: [
    TypeOrmModule.forFeature([
      ActionLog,
      PendingChange, // Add this
      // ... other entities
    ]),
  ],
  providers: [
    ActionLogBufferService,
    GenericChangelogService,
    ChangelogConfigFactory,
    // ... other providers
  ],
  exports: [
    ActionLogBufferService,
    // ... other exports
  ],
})
export class ActionLogModule {}

### 5. Update Controller/Service Usage

**Files**: Any controller or service that currently calls `ActionLogBufferService`

**Required Changes**:

#### A. Update `addChange` Calls

**Before** (approximate):
typescript
this.actionLogBufferService.addChange(
  { assessmentRequestId, assessmentLayerId },
  'testcase',
  beforeEntity,
  updateDto,
);

**After**:
typescript
await this.actionLogBufferService.addChange(
  { assessmentRequestId, assessmentLayerId },
  {
    entityType: 'testcase',
    beforeEntity,
    updateDto,
    userId: user.id,
    ipAddress: req.ip,
  },
);

**Key changes**:
- Add `await` (method is now async)
- Pass `userId` and `ipAddress` in details object
- Ensure `user` and `req` (or `ipAddress`) are available in context

#### B. Update `flushToActionLog` Calls

**Before** (approximate):
typescript
await this.actionLogBufferService.flushToActionLog(
  { assessmentRequestId, assessmentLayerId },
  {
    userId: user.id,
    roleIds: user.roleIds,
    action: ActionEnum.STATE_TRANSITION,
    // ... other fields
  },
);

**After**: Should remain mostly the same, but ensure:
- All required fields in `logData` are provided
- `ipAddress` is included if available
- State IDs are provided correctly

### 6. Create Cleanup Cron Job (Optional but Recommended)

**File**: `src/action-log/jobs/cleanup-pending-changes.job.ts`

Create a scheduled job to clean up old flushed records:

typescript
import { Injectable, Logger } from '@nestjs/common';
import { Cron, CronExpression } from '@nestjs/schedule';
import { ActionLogBufferService } from '../services/action-log-buffer.service';

@Injectable()
export class CleanupPendingChangesJob {
  private readonly logger = new Logger(CleanupPendingChangesJob.name);

  constructor(private actionLogBufferService: ActionLogBufferService) {}

  @Cron(CronExpression.EVERY_DAY_AT_2AM)
  async handleCleanup() {
    this.logger.log('Starting cleanup of flushed pending changes...');
    try {
      const deletedCount = await this.actionLogBufferService.cleanupFlushedChanges(7);
      this.logger.log(`Cleanup completed. Deleted ${deletedCount} records.`);
    } catch (error) {
      this.logger.error(`Cleanup failed: ${error.message}`, error.stack);
    }
  }
}

**Module registration**:
- Add `CleanupPendingChangesJob` to `ActionLogModule` providers
- Ensure `@nestjs/schedule` is installed and `ScheduleModule` is imported in `AppModule`

### 7. Testing Checklist

After implementation, verify:

1. **Basic Flow**:
   - [ ] Update a testcase → pending change saved to database
   - [ ] Update another testcase → another pending change saved
   - [ ] Trigger state transition → both changes flushed to single ActionLog
   - [ ] Verify `isFlushed = true` for both pending changes

2. **Restart Resilience**:
   - [ ] Add pending changes
   - [ ] Restart application (without flushing)
   - [ ] Trigger state transition
   - [ ] Verify pending changes are still retrieved and flushed

3. **Edge Cases**:
   - [ ] Flush with no pending changes → ActionLog created with no changes
   - [ ] Multiple requests with different IDs → changes isolated correctly
   - [ ] Layer-specific vs request-level changes → filtered correctly

4. **Cleanup**:
   - [ ] Manually trigger cleanup job
   - [ ] Verify old flushed records are deleted
   - [ ] Verify unflushed records are NOT deleted

5. **Error Handling**:
   - [ ] Database connection failure during `addChange` → error logged
   - [ ] Database connection failure during `flushToActionLog` → error logged

## Implementation Order

1. Create `PendingChange` entity
2. Create database migration
3. Run migration: `npm run migration:run`
4. Update `ActionLogModule` (add entity to TypeORM)
5. Update `ActionLogBufferService` (all methods)
6. Update all controller/service usages
7. Create cleanup cron job (optional)
8. Test thoroughly

## Important Notes

- **Scope**: Keep `ActionLogBufferService` as `@Injectable({ scope: Scope.DEFAULT })` (singleton), NOT `Scope.REQUEST`, since we're using database storage
- **Transactions**: Consider wrapping `flushToActionLog` in a database transaction for atomicity (optional enhancement)
- **Performance**: The indexes on `assessmentRequestId`, `assessmentLayerId`, and `isFlushed` are critical for query performance
- **Cleanup**: Without the cleanup job, the `pending_changes` table will grow indefinitely
- **Error Recovery**: If `flushToActionLog` fails after creating ActionLog but before marking as flushed, the same changes might be flushed again. Consider adding transaction or idempotency checks if this is a concern.

## Expected Outcome

After implementation:
- ✅ All pending changes persist across application restarts
- ✅ No data loss on crashes or deployments
- ✅ Works correctly with load balancers and multiple server instances
- ✅ Clean separation between buffering and logging
- ✅ Automatic cleanup of old data
- ✅ Full audit trail of all changes

## Code Quality Requirements

- Use TypeScript strict mode
- Add proper error handling with try-catch
- Add logging at debug and error levels
- Use async/await consistently
- Follow NestJS best practices
- Add JSDoc comments for public methods
- Use proper TypeORM query builders and operators

---

**Start implementation now. Create all files in order, ensuring each step is complete before moving to the next.**